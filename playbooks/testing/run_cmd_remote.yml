- name: "start remote-test"
  gather_facts: no
  hosts: all
  tasks:
    - shell: "whoami && hostname && ls -la"
      name: Shell Command
      register: cmd_out
    - debug: msg={{ cmd_out.stdout_lines }}
