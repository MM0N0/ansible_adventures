all:
  children:
    localhost_group:
      hosts:
        localhost:
          ansible_connection: local
    proj:
      vars:
        ansible_user: user
        ansible_password: user
        can_become_root: false
